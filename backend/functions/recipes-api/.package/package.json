{
  "name": "recipes-api",
  "private": true,
  "type": "commonjs",
  "scripts": {
  "zip": "powershell -NoProfile -ExecutionPolicy Bypass -File ./deploy.ps1",
  "deploy": "powershell -NoProfile -ExecutionPolicy Bypass -File ./deploy.ps1 --deploy",
    "fmt": "npx prettier -w .",
    "test": "node -e \"console.log('ok')\""
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.632.0",
    "@aws-sdk/lib-dynamodb": "^3.632.0"
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
