# Netlify redirects file for Silver Spoon Society
# This ensures proper routing for the website

# Handle root route by serving cta.html (landing page)
/    /cta.html   200

# Handle all other routes by serving index.html
/*    /index.html   200

# Optional: Handle specific API routes if you have backend endpoints
# /api/*  https://your-backend-url.com/api/:splat  200